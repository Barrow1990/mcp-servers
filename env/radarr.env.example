# Copy to radarr.env (this directory) and fill in your values.
# Radarr > Settings > General > API Key
RADARR_URL=http://192.168.1.50:7878
RADARR_API_KEY=replace-with-your-radarr-api-key

# Shared secret required as `Authorization: Bearer <token>` on every request
# to this server. Leave unset to run with no auth. openssl rand -hex 32
MCP_AUTH_TOKEN=
